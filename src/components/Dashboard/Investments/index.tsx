import React from 'react'

interface IInvestments {
    upperNavigation: string 
    setUpperNavigation: (child: string) => void
}

const Investments : React.FC<IInvestments> = ({upperNavigation, setUpperNavigation}) => {
  return (
    <div>
        
    </div>
  )
}

export default Investments